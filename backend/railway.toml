# Railway service settings (Root Directory must be `backend`).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Prefer this over uvicorn --port $PORT (Railway may not expand $PORT).
startCommand = "python -m app.run"
restartPolicyType = "ON_FAILURE"
